<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ggikko.me.pagingtestapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".first.FirstActivity" />
        <activity android:name=".second.SecondActivity" />
        <activity android:name=".third.ThirdActivity" />
        <activity android:name=".MainTwoActivity" />
        <activity android:name=".MainThreeActivity" />
        <activity android:name=".first.FirstTwoActivity" />
        <activity android:name=".first.FirstThreeActivity" />
        <activity android:name=".second.SecondTwoActvitiy" />
        <activity android:name=".third.ThirdTwoActivity"></activity>
    </application>

</manifest>